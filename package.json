{
  "name": "tinypint",
  "version": "1.0.0",
  "description": "A website for a start up beer taproom.",
  "scripts": {
    "dev": "tailwindcss -i ./style.css -o ./output.css --watch",
    "build": "tailwindcss -i ./style.css -o ./output.css"
  },
  "devDependencies": {
    "tailwindcss": "latest",
    "@tailwindcss/cli": "latest"
  }
}
